{
  "name": "faboxlous",
  "version": "0.1.1",
  "main": "build/index.js",
  "author": "Jim Filippou",
  "license": "MIT",
  "scripts": {
    "build": "rimraf lib && babel src -d build"
  },
  "dependencies": {
    "ffi": "^2.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "rimraf": "^2.6.2"
  }
}
